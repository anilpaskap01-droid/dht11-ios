name: dhst11
description: ESP32 DHT11 BLE monitor ve hava durumu uygulaması.
publish_to: "none"
version: 1.5.0+6

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: 1.36.8
  http: ^1.2.2
  shared_preferences: ^2.3.2
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
